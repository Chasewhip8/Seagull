import type { NextPage } from "next";
import Head from "next/head";

const Dashboard: NextPage = (props) => {
    return (
        <>
            <Head>
                <title>Seagull Finance</title>
                <meta
                    name="description"
                    content="Seagull Finance"
                />
            </Head>
        </>
    );
};

export default Dashboard;
